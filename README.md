# Rust Generic Input Helper 🦀

![Rust](https://img.shields.io/badge/language-Rust-orange)
![Version](https://img.shields.io/badge/version-1.0.0-blue)

A simple, engineered approach to handle user input in Rust using Generics.

## Features
- **Type Safe**: Uses Rust's type system to ensure valid data.
- **Looping**: Automatically retries on invalid input.
- **Generic**: Works with `i32`, `f64`, `bool`, etc., using a single function.

## How to Run
```bash
cargo run